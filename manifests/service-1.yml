apiVersion: v1
kind: Service
metadata:
    name: "k8stestprofileapp-52cc"
    labels:
        app: "k8stestprofileapp-52cc"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "k8stestprofileapp-52cc"